#include <iostream>
using namespace std;
int main()
{
	int P, Q, R, S,max= -100000 ;


	cout <<"enter four numbers: ";
	cin >> P >> Q >> R >> S;
	if (P>max)
		max=P;
	if (Q>max)
		max=Q;
	if (R>max)
		max=R;
	if (S>max)
		max=S;
	cout << max << endl;

	return 0;
} 
